// Use with the agent-deck alias from zellij.kdl.
layout {
    default_tab_template {
        children
        pane size=1 borderless=true {
            plugin location="agent-deck" {
                display "status"
            }
        }
    }
    tab {
        pane
    }
}
